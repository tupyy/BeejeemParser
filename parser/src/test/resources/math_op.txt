int a = 4;
int b=1;

float c = a / (b*4); // = 1
float cc = a * (b*4)/4 + 4; // = 8
float p = a ^ b; // = 4
int i1 = a * (b * (a+b+1)); // = 24

// test boolean operations

bool b = true;
bool b1 = !b; // false